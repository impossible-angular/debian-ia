# Apps and Config for Debian

## Install

Run unde root

```bash
apt update;
apt install -y git;
git clone https://github.com/impossible-angular/debian-ia.git;
chmod -R 744 ./debian-ia
cd ./debian-ia;
./index.sh;
```